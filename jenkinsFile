  
pipeline {
    agent any 
    tools
   {
    maven 'Maven'
    jdk 'JDK8'
   }
    stages {
       
        stage('Test') { 
            steps {
			    checkout scm
                bat "git checkout branch ${branch-name}"
                bat "mvn clean" 
            }
        }
        
        }
    }
