  
pipeline {
    agent any 
    tools
   {
    maven 'Maven'
    jdk 'JDK8'
   }
   
   
   
 //  parameters {
  //      string(name: 'branch-name', defaultValue: 'master', description: 'which branch you want to checkout?')}
    stages {
       
        stage('Test') { 
            steps {
			    checkout scm
		
		}
			   steps{
			   script{
			  // echo "my branch is $branch-name"
                bat "git branch"
                bat "mvn clean" 
		
		
            }}
        }
        
        }
    }
